# Public domain
SITE_DOMAIN=linuxunity.com
SITE_WWW_DOMAIN=www.linuxunity.com
NEXT_PUBLIC_SITE_URL=https://linuxunity.com

# Caddy / Let's Encrypt
CADDY_EMAIL=admin@linuxunity.com
CADDY_MAX_UPLOAD_SIZE=64MB

# WordPress public URL. Keep this as the same-domain HTTPS URL because
# Caddy routes /wp-admin, /wp-json, and WordPress assets to the WordPress container.
WORDPRESS_SITE_URL=https://linuxunity.com

# WordPress database credentials
WORDPRESS_DB_NAME=wordpress
WORDPRESS_DB_USER=wordpress
WORDPRESS_DB_PASSWORD=change-this-wordpress-db-password
MARIADB_ROOT_PASSWORD=change-this-mariadb-root-password
WORDPRESS_TABLE_PREFIX=wp_
WORDPRESS_DEBUG=0
WORDPRESS_MEMORY_LIMIT=256M

# Next.js server-side WordPress REST API target inside Docker.
# The public browser never sees this value.
WORDPRESS_API_BASE=http://wordpress/wp-json/wp/v2

# Optional scripts: import/list roadmap drafts through the public WordPress REST API.
# Use a WordPress Application Password, not your normal login password.
WORDPRESS_URL=https://linuxunity.com
WORDPRESS_USERNAME=admin-user
WORDPRESS_APP_PASSWORD=xxxx xxxx xxxx xxxx xxxx xxxx

# Keep false for normal operation. If true, the import script still requires
# WORDPRESS_DELETE_CONFIRM=delete-posts-only before deleting WordPress posts.
WORDPRESS_DELETE_EXISTING_POSTS=false
WORDPRESS_DELETE_CONFIRM=
WORDPRESS_WRITE_META=false
ROADMAP_WORDPRESS_MANIFEST=content/roadmap-draft-posts.wordpress.json

# Optional WP-CLI helper variables for first admin creation/reset.
# Do not commit real values in .env.
WP_ADMIN_USER=admin
WP_ADMIN_PASSWORD=change-this-wordpress-admin-password
WP_ADMIN_EMAIL=admin@linuxunity.com
